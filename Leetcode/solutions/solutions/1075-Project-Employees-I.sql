# Write your MySQL query statement below
SELECT PT.PROJECT_ID AS project_id,SUM(EXPERIENCE_YEARS)/ COUNT(PT.EMPLOYEE_ID) as average_years FROM EMPLOYEE ET  JOIN PROJECT PT ON ET.EMPLOYEE_ID = PT.EMPLOYEE_ID GROUP BY PT.PROJECT_ID  ;